#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,k;
	cin >> n >> k;
	int p = n;
	int sum = 0;
	float avg = 0.00;
	for(int i = 1 ; i <= n ; i++)
	{
		int a;
		cin >> a;
		sum = sum + a;

	}
	avg = sum/n;

	while(ravg < k)
	{
		sum =sum + k;
	}
}