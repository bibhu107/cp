#include  <bits/stdc++.h>
using namespace std;
int main()
{
	
	int t;
	cin>>t;
	while(t--)
	{
		int n,q;
		cin>>n>>q;
		for(int i=0;i<q;i++)
		{
			cin>>a>>b>>ele;
			mypair[i].push_back(make_pair(a,b));
			first.push_back(a);
			values.push_back(ele);
		}
	}






	if(binary_search(v.begin(),v.end(),b))
		cout<<lower_bound(v.begin(),v.end(),b)-v.begin();
		
}